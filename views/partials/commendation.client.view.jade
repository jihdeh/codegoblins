md-dialog.commend-dialog(data-ng-controller='publicProfile')
  md-toolbar.md-primary
    h1.md-toolbar-tools
      | Top Notch
  md-content.md-padding
    form(name='commendForm')
      md-input-container
        label Topic
        input(required='', name='topic', ng-model='topic', md-maxlength='40')
        {{clientNames}}
        div(ng-messages='commendForm.topic.$error')
          div(ng-message='required') This is required.
          div(ng-message='md-maxlength') The name has to be less than 40 characters long.
      md-input-container(flex='')
        label Commendation
        textarea(name='commentArea', required='' ng-model='commend', columns='1', placeholder='Your comments if you will recommend this developer of not?', md-maxlength='100')
        div(ng-messages='commendForm.commentArea.$error')
          div(ng-message='required') This is required.
          div(ng-message='md-maxlength') The name has to be less than 100 characters long.
    section(layout='row', layout-sm='column', layout-align='center center')
      md-button.md-raised.commend-modal-btn(ng-click='closeDialog()') Cancel
      md-button.md-raised.md-primary.commend-modal-btn(ng-click='sendCommend()', ng-disabled='!commendForm.$valid') Submit